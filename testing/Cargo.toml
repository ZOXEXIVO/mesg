[package]
name = "mesg-integration-testing"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1.32.0", features = ["full"] }
tonic = "0.10.0"
prost = "0.12.0"

[build-dependencies]
tonic-build = "0.10.0"
